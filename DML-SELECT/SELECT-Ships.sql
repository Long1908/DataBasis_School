--task1
SELECT CLASS, COUNTRY
FROM CLASSES
WHERE NUMGUNS < 10;

--task2
SELECT NAME as shipName
FROM SHIPS
WHERE LAUNCHED < 1918;

--task3
SELECT SHIP, BATTLE
FROM OUTCOMES
WHERE RESULT = 'sunk';

--task4
SELECT NAME
FROM SHIPS
WHERE NAME = CLASS;

--task5
SELECT NAME
FROM SHIPS
WHERE NAME LIKE 'R%';

--task6
SELECT NAME
FROM SHIPS
WHERE NAME LIKE '% %'
AND NAME NOT LIKE '% % %';